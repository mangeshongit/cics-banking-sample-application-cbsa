name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Build webui module
      run: mvn clean install -pl src/webui -am

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v3
      with:
        role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
        aws-region: us-east-1
        role-session-name: github-actions-${{ github.run_id }}

    - name: Upload WAR to S3
      run: |
        set -e
        aws s3 mb s3://cbsa-deployment-bucket --region us-east-1 || true
        aws s3 cp src/webui/target/*.war s3://cbsa-deployment-bucket/webui-v${{ github.run_number }}.war

    - name: Create EB application version
      run: |
        set -e
        aws elasticbeanstalk create-application-version \
          --application-name cbsa-webui \
          --version-label v${{ github.run_number }} \
          --source-bundle S3Bucket=cbsa-deployment-bucket,S3Key=webui-v${{ github.run_number }}.war \
          --auto-create-application \
          --region us-east-1

    - name: Update EB environment
      id: deploy
      continue-on-error: true
      run: |
        set -e
        aws elasticbeanstalk update-environment \
          --environment-name cbsa-webui-env \
          --version-label v${{ github.run_number }} \
          --region us-east-1
    - name: Rollback on failure
      if: steps.deploy.outcome == 'failure'
      run: |
        echo "Deployment failed. Rolling back to previous version..."
        PREV_VERSION=$(aws elasticbeanstalk describe-environments \
          --environment-names cbsa-webui-env \
          --query "Environments[0].VersionLabel" \
          --output text \
          --region us-east-1)

        echo "Previous version: $PREV_VERSION"

        aws elasticbeanstalk update-environment \
          --environment-name cbsa-webui-env \
          --version-label "$PREV_VERSION" \
          --region us-east-1